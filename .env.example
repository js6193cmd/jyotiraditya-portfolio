# Required only for live AI mode. Without this, the portfolio uses grounded demo mode.
OPENAI_API_KEY=

# Optional. The site defaults to gpt-5.6-luna when OPENAI_API_KEY is set.
OPENAI_MODEL=gpt-5.6-luna

# Optional. Raises GitHub API limits for the live GitHub section.
GITHUB_TOKEN=
